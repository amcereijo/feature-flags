feature-flags/bun-api/.env.test
# Environment variables for bun-api integration tests

# JWT secret used for signing and verifying tokens in tests
JWT_SECRET=supersecret

# Admin token for test authentication (replace with a valid token generated using the same JWT_SECRET)
ADMIN_TOKEN=replace_with_your_generated_admin_token

BYPASS_AUTH=true
